{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {},
  "devDependencies": {
    "lerna": "^8.0.2"
  },
  "scripts": {
    "dev:client": "lerna run start:dev:client",
    "dev:shared": "lerna run start:dev:shared",
    "dev:server": "lerna run start:dev:server"
  }
}
